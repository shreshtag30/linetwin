.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
uv.lock.tmp
*.pdf.tmp
.DS_Store
node_modules/

# Regenerable (tools/generate_training_data.py), ~17MB -- not worth carrying
# in git history. ml/models/ (the small, trained artifacts the live scorer
# actually loads at runtime) IS committed -- see tools/train_station_risk.py.
ml/data/

# For the user's own local tracking, not for the repo/submission.
docs/phases/*.pdf
docs/tracker.html
docs/INTERVIEW.md
